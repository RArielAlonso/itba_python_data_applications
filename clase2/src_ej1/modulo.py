import sys
import pandas as pd
import numpy as np
import datetime
import os

python_version=sys.version
pandas_version=pd.__version__
numpy_version=np.__version__

#print(f'La version de python es: {python_version}')
#print(f'La version de pandas es: {pandas_version}')
#print(f'La version de numpy es: {numpy_version}')

print(sys.modules.keys())